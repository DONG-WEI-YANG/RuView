version: "3.8"
services:
  wifi-body:
    build: .
    ports:
      - "8000:8000"
      - "5005:5005/udp"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - SIMULATE=true
    command: python -m server --simulate
